/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {

    images: {
        loader: 'default',
         unoptimized: true,
      },

    output: 'export',
  };
  export default nextConfig;




