module.exports = {
  swcMinify: false,
  experimental: {
    // Remove or comment out the invalid property if present
    // images: {}, 
  },
  output: 'export',
  images: { 
    unoptimized: true,
  },
};
